= Checked exceptions

This module adds checked versions of a number of common unchecked exceptions we have.

We can add these temporarily as we do not expect developers to be forced to handle these exceptions, but we want to ensure:

* we handle these correctly in our code.
* we handle these consistently with the right exception, and making these checked helps review the choices made.
* we document when these unchecked exceptions can or cannot occur.
