/**
 * Checked exception counterparts for selected {@code java.nio} types.
 *
 * <p>The classes in this package mirror a subset of the buffer
 * related exceptions from the standard library, but implement them as
 * checked exceptions. They are used alongside the custom
 * {@code java.lang} types in the checked exceptions module.
 *
 * <p>This package is internal to Chronicle Core and is not intended
 * to be used as a drop in replacement for the JDK {@code java.nio}
 * package.
 */
package java.nio;

